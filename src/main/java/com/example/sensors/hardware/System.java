package com.example.sensors.hardware;

/*
    TODO: Create a singleton representing a sensor system with List of CentralUnit containing a List of Sensor.
        Invoke the system on application startup.
        Investigate if this is how it should be done.
 */
public class System {

    public System() {
    }
}
